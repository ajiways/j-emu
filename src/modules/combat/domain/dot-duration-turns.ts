/**
 * Authored kind-4/5 tick counts that override duration/period.
 * Live 396 «трех ходов»; blob duration 81 with named period 20 would be 4.
 */
export const DOT_DURATION_TURNS: Readonly<Record<number, number>> = Object.freeze({
  396: 3,
});
