/** Fixture dumps omit hunt_mask; every current slice dungeon uses this mesh. */
export const DUMP_HUNT_MASK = "bot_1";

/** jgr parseSpawn/parseRoute and the ogre slice spawn wait when the fixture omits them. */
export const DUMP_SPAWN_WAIT_MIN = 2;
export const DUMP_SPAWN_WAIT_MAX = 8;
