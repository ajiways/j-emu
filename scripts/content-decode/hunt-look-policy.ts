/** Live `DEFAULT_HUNT_LOOK` when a catalog bot has no map sprite overlay. */
export const UNMAPPED_HUNT_SCALE = 60;
export const UNMAPPED_HUNT_FPS = 15;
export const UNMAPPED_HUNT_SPEED = 10;

/** Named empty-book policy from `bot_spell_book.json` defaults. */
export const EMPTY_BOT_SPELL_NOTHING_WEIGHT = 100;
